import { SAXParser } from 'parse5';
import { ParserState } from './parser-state';
import { Rule } from './rule';
import { AST } from './ast';
export declare class Parser extends SAXParser {
    state: ParserState;
    constructor(state: ParserState);
    isVoid(name: string): boolean;
    isScope(name: string): boolean;
    init(rules: Rule[], ast: AST, path: string): void;
    finalise(): void;
}
