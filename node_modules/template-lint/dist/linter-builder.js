"use strict";
const linter_1 = require("./linter");
class LinterBuilder {
    constructor(parserBuilder) {
        this.parserBuilder = parserBuilder;
    }
    withRule(rule) {
        this.rules.push(rule);
        return this;
    }
    withRules(rules) {
        this.rules.concat(rules);
        return this;
    }
    build() {
        var result = new linter_1.Linter(this.rules, this.parserBuilder);
        this.reset();
        return result;
    }
    reset() {
        this.rules = [];
    }
}
exports.LinterBuilder = LinterBuilder;

//# sourceMappingURL=linter-builder.js.map
