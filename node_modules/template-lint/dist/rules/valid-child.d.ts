import { Rule } from '../rule';
import { Parser } from '../parser';
export declare class ValidChildRule extends Rule {
    rules: {
        element: string;
        allow?: string[];
        exclude?: string[];
    }[];
    constructor(rules: {
        element: string;
        allow?: string[];
        exclude?: string[];
    }[]);
    init(parser: Parser): void;
}
