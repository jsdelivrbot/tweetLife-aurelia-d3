import { Rule } from '../rule';
import { Parser } from '../parser';
export declare class IdAttributeRule extends Rule {
    private ids;
    allowEmptyId: boolean;
    allowDuplicateId: boolean;
    allowIllegalChars: boolean;
    ignoreAny: RegExp;
    constructor(opts?: {
        allowEmptyId: boolean;
        allowDuplicateId: boolean;
        allowIllegalChars: boolean;
        ignoreAny: RegExp;
    });
    init(parser: Parser): void;
}
