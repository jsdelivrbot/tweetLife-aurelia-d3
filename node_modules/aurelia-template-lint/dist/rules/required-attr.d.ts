import { Rule } from 'template-lint';
import { Parser } from 'template-lint';
/**
 * Rule to ensure elements have required attributes
 */
export declare class RequiredAttributeRule extends Rule {
    patterns: Array<{
        tag: RegExp;
        attr: RegExp;
        msg: string;
    }>;
    constructor(patterns?: Array<{
        tag: RegExp;
        attr: RegExp;
        msg: string;
    }>);
    init(parser: Parser): void;
}
